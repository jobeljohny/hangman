export enum GameConfig {
  LIVES = 6,
  GAME_TIME = 600,
}

export enum Vals {
  ERROR = '#FF0000',
  CORRECT = '#32CD32',
  BLACK = '#000000',
  WHITE = '#FFFFFF',
  NORMAL = WHITE,
  PANEL_DEFAULT_MSG = 'Press the key you wish to guess !',
  CORRECT_MSG = 0,
  INCORRECT_MSG = 1,
  ERRORLIST_MSG = 2,
  BLINK_TIMER = 600,
}
export enum Result {
  PASSED = 'PASSED',
  FAILED = 'FAILED',
}

export enum Login {
  LOGIN = 'LOGIN',
  SIGNUP = 'SIGNIN',
}
